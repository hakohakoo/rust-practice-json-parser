mod libs;

// use libs::lexer::{Lexer, Token, TokenType};

fn main() {
    // let mut lex1 = Lexer::new(
    //     r#"{"aa1  23wd2": 123, "b": [true, false, null], "c": { "c1": 100, "c2": false }}"#,
    // );
    // lex1.generate();
    // println!("{:#?}", lex1.token_list);
    // let mut parser = Parser::new(lex1.token_list);
    // parser.generate();
    // println!("{:#?}", parser.ast);
}
